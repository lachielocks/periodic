import { LabApp } from "@/components/lab/LabApp";

export const metadata = {
  title: "Lab — Periodic",
  description:
    "Build chemically believable molecules with valence rules, VSEPR geometry, and bond validation.",
};

export default function LabPage() {
  return <LabApp />;
}
